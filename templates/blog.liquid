<h1>{{ blog.title }}</h1>
<h2>hello</h2>
{% for article in blog.articles %}
  <div>
    <a href="{{aritcle.url}}">
      <h2>{{ article.title }}</h2>
      <p>{{ article.author }}</p>
      <p>{{ article.excerpt }}</p>
    </a>
  </div>
{% endfor %}