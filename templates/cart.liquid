{% form 'cart', cart %}
  <h2>Cart Hieu</h2>
  {% for item in cart.items %}
    <div>
      <img src="{{item.image | image_url: width: 200}}" alt="">
      <h2>
        {{ item.title }}
      </h2>
      <h3>
        {{ item.price | money }}</h3>
        <p>Number item: {{ item.quantity }}
        <input name="update[]" value="{{ item.quantity }}">
        <a href="{{ item.url_to_remove }}">Remove</a>
    </div>
  {% endfor %}
  <hr/>
  <p>Number item: {{ cart.item_count }}
  </p>
  <p>Total: {{ cart.total_price | money }}</p>
  <button type="submit">Checkout</button>
{% endform %}